# Invoice form options (suggestions shown when creating invoices)
# 1. Copy this file to .env:  cp .env.example .env
# 2. Replace the example values below with your own. Do not commit .env.
# Use comma-separated values. Values with spaces are fine (e.g. "7 days").
# Payment term is set per client in the Clients page, not here.

# Unit suggestions for line items (e.g. days, hours)
VITE_UNITS=days,hours

# Rate suggestions in euros (comma-separated numbers)
VITE_RATES=75.00,100.00

# Group name suggestions (line items with same group are grouped in the PDF)
VITE_GROUP_NAMES=Services,Expenses

# Description suggestions per group: "GroupName:opt1,opt2;OtherGroup:opt1"
# Use semicolon between groups; colon after group name; comma between options.
# Leave a group empty (GroupName:) to use dynamic "previous month" suggestion for that group name.
VITE_DESCRIPTION_SUGGESTIONS=Expenses:Two-way ticket City A - City B,One-way ticket City A - City B;Services:Sprint
